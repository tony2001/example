// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("example", "for example support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("example", "enable example support", "no");

if (PHP_EXAMPLE != "no") {
	EXTENSION("example", "example.c", PHP_EXTNAME_SHARED, "/DZEND_ENABLE_STATIC_TSRMLS_CACHE=1");
}

